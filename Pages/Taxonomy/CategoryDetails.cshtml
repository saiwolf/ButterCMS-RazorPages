@page "{slug}"
@model SWMNU_NET.Pages.Taxonomy.CategoryDetailsModel
@{
    ViewData["Title"] = "CategoryDetails";
}

<div>
    <a href="/" title="Go to Home Page"><i class="fas fa-arrow-left"></i> Back Home</a>
</div>

<h2>Posts in @Model.Category.Category.Name</h2>

@Html.DisplayFor(m => m.Blogs.Posts)

@if (Model.Blogs.TotalPages > 1)
{
    if (Model.Blogs.PreviousPage != null)
    {
        <a href="/category/@Model.Category.Category.Slug/p/@Model.Blogs.PreviousPage">Prev</a>
    }

    for (int i = 1; i <= Model.Blogs.TotalPages; i++)
    {
        if (i == Model.Blogs.CurrentPage)
        {

            <span class="page-number active">@i</span>
        }
        else
        {
            <span class="page-number"><a href="/category/@Model.Category.Category.Slug/p/@i">@i</a></span>
        }

    }

    if (ViewBag.NextPage != null)
    {
        <a href="/category/@Model.Category.Category.Slug/p/@Model.Blogs.NextPage">Next</a>
    }
}

